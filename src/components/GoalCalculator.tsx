// GoalCalculator removed — kept a small stub to avoid build errors while
// removing the full implementation. If you want the file deleted entirely,
// I can remove it once you confirm.
export default function GoalCalculator() {
  return null;
}
